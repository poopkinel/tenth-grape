export enum GamingFrequency {
  CASUAL = 'CASUAL',
  REGULAR = 'REGULAR',
  HEAVY = 'HEAVY',
}

export enum GameOwnership {
  OWN = 'OWN',
  WISHLIST = 'WISHLIST',
  WILLING = 'WILLING',
}

export enum MatchStatus {
  PENDING = 'PENDING',
  MATCHED = 'MATCHED',
}

export enum SessionParticipantStatus {
  INVITED = 'INVITED',
  ACCEPTED = 'ACCEPTED',
  DECLINED = 'DECLINED',
}
